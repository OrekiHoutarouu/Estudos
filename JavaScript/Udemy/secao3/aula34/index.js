const frutas = ["Pêra", "Maçã", "Uva"]

for (let c in frutas) {
    console.log(frutas[c])
}

const pessoa = {
    nome: "Samuel",
    sobrenome: "Campelo",
    idade: 30
}

for (let c in pessoa) {
    console.log(c, pessoa[c])
}